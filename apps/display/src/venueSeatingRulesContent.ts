/** Copy for the public-display seating & tables explainer (hosts + players). */
export const VENUE_SEATING_RULES_HEADLINE = 'Tables & seating'

export const VENUE_SEATING_RULES_LINES = [
  'When the host assigns seating, everyone is shuffled randomly into tables.',
  'As the field shrinks, the room may shuffle a few people or combine tables.',
] as const
